declare module 'host/SharedService' {
  export class SharedService {
    data$: any; // Replace `any` with the actual type of `data$`
    setData(data: any): void; // Replace with actual method signatures
  }
}